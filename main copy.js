// // ドキュメントが読みこまれたら実行する関数を定義
// window.onload = () => {
//   const up10game = games.filter((game) => game.rank <= 10);
//   up10game.forEach((game) => {
//     // console.log(game);
//     addChart(game);
//   });
// };

// const addChart = (game) => {
//   // $("#xxx").text(game.name);
//   // console.log(game.name);

//   const parent = $("#chartTable");
//   const li = $("<li>", { text: game.name });
//   parent.append(li);

//   // $("#station-info-title").text(station.name);
// };
